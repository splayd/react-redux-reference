{
  "license": "MIT",
  "scripts": {
    "test": "standard -v --fix && ava"
  },
  "devDependencies": {
    "ava": "^0.22.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "babel-register": "^6.26.0",
    "standard-esnext": "^4.0.1"
  },
  "ava": {
    "babel": "inherit",
    "require": [
      "babel-register"
    ]
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "current"
          }
        }
      ],
      "stage-0"
    ]
  }
}
